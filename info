Follw these steps to copy the project.

1.Create a new android project in eclipse with projec name "applefactorfart".
2.Copy the contents of directory 'code' to that projrct directory(replace all).

you are done.

you can export the project using "export" option in eclipse,also you can just run the app on android virtual device(emulator) 
or a real device(https://developer.android.com/training/basics/firstapp/running-app.html).

please ignore the directory "new".
